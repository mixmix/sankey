{
  "name": "sankey",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js -o public/bundle.js",
    "buildify": "watchify index.js -o public/bundle.js",
    "push": "gh-pages -d public",
    "deploy": "npm run build && npm run push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.0.1",
    "gh-pages": "^0.11.0",
    "watchify": "^3.7.0"
  }
}
